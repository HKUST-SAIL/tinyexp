coverage:
  range: 50..100
  round: down
  precision: 1
  status:
    project:
      default:
        # Current baseline is intentionally modest; see `pyproject.toml` coverage configuration for details.
        target: 55%
        threshold: 1%
    patch:
      default:
        target: 80%
        threshold: 1%
